#include "Scene.h"


/*

void Scene::addLight(std::shared_ptr<Light> light)
{
    m_lights.push_back(light);
}

void Scene::applyLightToShaders(const Shader& shader)
{
    shader.use();
    shader.setUniformVec3("lightColor", m_lights[0]->getColor());
    shader.setUniformVec3("lightPos", m_lights[0]->getPosition());
}

 */
